# Generated by Cargo — this crate has its own target dir since it's
# excluded from the main workspace (see root Cargo.toml).
/target/

# Scratch output from capture.ps1/shot.ps1 and from redirecting `cargo run`
# while iterating on the visuals. The `screenshot-*.png` files the README
# embeds are committed on purpose, so this ignores the capture prefix
# rather than all PNGs.
/capture*.png
*.log
