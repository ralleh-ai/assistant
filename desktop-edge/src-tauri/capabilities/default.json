{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal Phase 1 capability — core IPC only (T11)",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
